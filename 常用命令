sudo rm /var/lib/dpkg/lock-frontend

sudo apt-get -f install
sudo dpkg -i /home/cl/linuxidc.deb

// 卸载
sudo apt-get remove
sudo apt-get autoremove
sudo apt-get autoremove <name>*
sudo apt-get autoremove <name> --purge

// 清理依赖(有问题不要用)
sudo apt autoremove

// 删除非空目录
sudo rmdir

// 强制删除
sudo rm -rf

// 显示隐藏文件
ctrl+h

找不到库(canberra-gtk-module)

输入
~$ locate libcanberra-gtk-module.so

在/usr/lib/x86_64-linux-gnu/gtk-3.0/modules/libcanberra-gtk-module.so有安装

建立连接
~$ sudo ln -s /usr/lib/x86_64-linux-gnu/gtk-3.0/modules/libcanberra-gtk-module.so /usr/lib/libcanberra-gtk-module.so

安装popcorn-time时缺少的库
sudo apt-get install gconf
sudo apt-get install --reinstall libcanberra-gtk-module
sudo apt-get install appmenu-gtk2-module appmenu-gtk3-module



